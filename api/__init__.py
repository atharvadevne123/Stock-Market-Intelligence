"""FastAPI application package for Stock Market Intelligence.

Exposes the ASGI `app` object for WSGI/ASGI servers and
provides the `/health`, `/api/version`, `/api/metrics`,
`/api/analyze`, `/api/signals`, `/api/technical`, and
`/api/portfolio` endpoints.
"""
from __future__ import annotations

__version__ = "1.0.0"
